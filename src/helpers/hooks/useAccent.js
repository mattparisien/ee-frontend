// import { useEffect, useState } from "react";
// import $ from "jquery";

// export default function useAccent(text, arrayOfWords) {
// 	//text: the text to be analyzed
// 	//arrayOfWords: an array of words to be wrapper in text

// 	const [accentedText, setAccentedText] = useState(null);

// 	const textArray = text.split(" ");

//   for (let i = 0; i < textArray.length; i++) {
//     if (arrayOfWords.includes(textArray[i])) {
//       console.log(textArray[i].wrap("<div></div>"))
      
//     }
    
//   }

  
// 	return [accentedText];
// }
