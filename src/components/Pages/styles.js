import styled from "styled-components";

export const StyledFeaturedWork = styled.div`


ul {
  z-index: 2;
  position: sticky;
}

.featured-work-free-image-wrapper {
  
  
  position: absolute;
  height: 30vw;
  width: 400px;
  bottom: 0;
  left: 0;
  transform: translate(-50%, -50%);
  z-index: 1;
  
  
}
`
